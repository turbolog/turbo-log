import axios from "axios";

const initialState = {
  username: ""
};

//action types

//action creators

//reducer
export default function authReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
